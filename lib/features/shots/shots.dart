import 'package:flutter/material.dart';

class ShotsPage extends StatelessWidget {
  const ShotsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shots'),
      ),
      body: const Center(
        child: Text('Shots page coming soon'),
      ),
    );
  }
}
