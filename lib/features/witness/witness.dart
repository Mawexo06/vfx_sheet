import 'package:flutter/material.dart';

class WitnessPage extends StatelessWidget {
  const WitnessPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Witness'),
      ),
      body: const Center(
        child: Text('Witness page coming soon'),
      ),
    );
  }
}
