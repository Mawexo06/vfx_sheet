import 'package:flutter/material.dart';

class VpLedPage extends StatelessWidget {
  const VpLedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('VP LED'),
      ),
      body: const Center(
        child: Text('VP LED page coming soon'),
      ),
    );
  }
}
