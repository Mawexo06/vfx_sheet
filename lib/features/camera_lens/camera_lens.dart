import 'package:flutter/material.dart';

class CameraLensPage extends StatelessWidget {
  const CameraLensPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Camera & Lens'),
      ),
      body: const Center(
        child: Text('Camera & Lens page coming soon'),
      ),
    );
  }
}
