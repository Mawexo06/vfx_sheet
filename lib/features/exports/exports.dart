import 'package:flutter/material.dart';

class ExportsPage extends StatelessWidget {
  const ExportsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exports'),
      ),
      body: const Center(
        child: Text('Exports page coming soon'),
      ),
    );
  }
}
